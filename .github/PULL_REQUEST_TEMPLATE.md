## What

<!-- One or two sentences: what does this PR change and why? -->

## Checklist

- [ ] Tests added or updated (lint rules use fixtures; deploy uses temp dirs)
- [ ] `npm run typecheck && npm test` passes
- [ ] Docs updated (`docs/lint-rules.md` for rules, `docs/deploy-targets.md` for targets, `docs/secrets.md` for providers)
- [ ] `examples/acme-saas` and both archetypes still lint clean, if behavior changed
- [ ] Convention changes went through an RFC issue first

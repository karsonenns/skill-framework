# Lint configuration — per-rule severity (error | warn | off) and budget
# overrides. Rules keep their defaults unless listed here.
# See docs/lint-rules.md for every rule id.
rules: {}
  # SF007: error     # make description quality blocking
  # SF015: warn      # flag unknown frontmatter fields

budgets: {}
  # skill_tokens: 1500
  # tree_tokens: 40000
